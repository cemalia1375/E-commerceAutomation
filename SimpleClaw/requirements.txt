# SimpleClaw / Mojing API 依赖
# 安装：pip install -r requirements.txt

# Web 框架
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
starlette>=0.40.0
python-multipart>=0.0.9  # /admin/lab zip 上传（FastAPI Form/File）
openpyxl>=3.1.2          # /admin/scenario Excel 回放样本导入

# 数据库
aiomysql>=0.2.0
PyMySQL>=1.1.0

# LLM
openai>=1.50.0
google-genai>=2.0.0

# 账号体系（密码哈希）
bcrypt>=4.0.0

# 工具库
loguru>=0.7.0
python-dotenv>=1.0.0
pydantic>=2.5.0
httpx>=0.27.0
aiohttp>=3.9.0
json-repair>=0.20.0
scenedetect[pyav]>=0.6.4

# 对象存储（火山引擎 TOS：Flowcut 素材 + /admin/lab 照片回填直传）
tos>=2.9.0

# 向量搜索
qdrant-client>=1.12.0,<2.0.0

# 任务队列 / Cron
redis>=5.0.0
croniter>=2.0.0


# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.0
